describe('MeetupService', function() {

    beforeEach(function() {
        module('app', function($provide) {});
    });
    
    it('should', inject(
        function() {
            
        }
    ));
});
